# AI-NEWS 项目代码审查 (更新版)

本文档旨在为项目的核心功能和逻辑提供一份全面的审查报告。

## 1. 新闻获取配置

- **你的描述**: 系统应只获取1条ABC新闻，0条BBC新闻。
- **核实结果**: ✅ **正确**。
- **详情**: `src/config/index.ts` 文件中的配置与你的描述一致。

## 2. 默认辩论轮数和脚本规模

- **你的描述**: 系统默认生成一轮对话（4个音频片段）。
- **核实结果**: ❌ **不正确**。
- **详情**: 系统默认生成 **3轮** 辩论，产生 **8个** 音频片段。这是因为 `` 方法的 `debateRounds` 参数默认为3。为实现1轮对话，前端必须在调用API时明确传递 `{"debateRounds": 1}`。

## 3. 顺序播放逻辑

- **你的描述**: 音频成功生成后，应按顺序播放。
- **核实结果**: ✅ **正确**。
- **详情**: 后端的任务状态流转 (`READY_TO_PLAY`) 和接口 (`getNextPlayableTask`) 已支持前端实现顺序播放。

## 4. TTS 任务重试逻辑分析 (新增)

- **你的描述**: TTS任务在失败3次后应该停止，但日志显示它在持续重试。
- **核实结果**: ✅ **你的观察是正确的**。系统存在一个设计缺陷，导致了无限重试循环。

- **问题分析**: 
    系统中存在两个重试机制：
    1.  **低层重试 (正常)**: `IndexTTS-Client` 在请求单个音频片段时，如果网络失败，会重试最多3次。这个机制工作正常。
    2.  **高层重试 (缺陷)**: `PipelineOrchestrator` 在执行整个音频批量生成任务 (`executeTTSTask`) 时，如果任务因任何原因（如超时）失败，`catch` 块会捕获错误，然后**无条件地将任务状态重置回 `PENDING_AUDIO`**。

- **根本原因**: 高层调度器没有自己的重试计数器。它每次失败后都将任务重新放回待处理队列，导致下一次调度时再次从头执行，形成无限循环。它应该像文本生成任务 `executeAITask` 那样，拥有自己的重试限制。

- **修复状态**: ✅ **已修复** (2025-09-16)
    在新的解耦架构中，已完全移除音频生成的高层重试机制：
    -   `AudioGenerationService` 不再返回 `shouldRetry` 标志
    -   `PipelineScheduler` 中音频生成失败时直接标记为 `FAILED` 状态
    -   保留了文本生成的有限重试机制（最多3次）
    -   音频生成仍保留低层重试（IndexTTS-Client层面），但高层不再重试
