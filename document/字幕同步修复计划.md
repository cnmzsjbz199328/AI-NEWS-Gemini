# 字幕、语音、动态图片同步修复计划 ✅ 已完成

## 当前问题分析

### 1. 主要问题
- **字幕过早显示**：第一个发言人（主持人）还没播放语音，字幕就出现了 ✅ 已修复
- **动画状态不一致**：图片是动态的，但当语音出现时图片反而变成静态的 ✅ 已修复
- **字幕显示时机不对**：字幕应该只在语音播放时显示，而不是在文本生成完成时显示 ✅ 已修复

### 2. 当前代码流程分析
```
原问题流程：
1. executeSpeakerTurn() 开始
2. generateText() - 生成文本
3. updateConversation() - 立即更新对话记录 ❌ 问题所在
4. generateAudio() - 生成语音
5. AudioManager.playAudio() - 播放语音
6. setSpeaking() - 设置角色为发言状态
```

### 3. 修复后的正确流程 ✅
```
新的正确流程：
1. executeSpeakerTurn() 开始
2. generateText() - 生成文本
3. setThinking() - 设置思考状态（动态图片）
4. generateAudioWithCallback() - 生成语音（带回调）
5. AudioManager.playAudio() - 开始播放语音
6. onPlaybackStart() - 语音播放时回调，更新对话记录 ✅
7. setSpeaking() - 设置发言状态（动态图片）
8. 字幕显示基于 speaking 状态 ✅
9. onAudioComplete() - 语音结束，设置静态状态
```

## 修复方案 ✅ 已实施

### 方案1：修改对话记录更新时机 ✅
- ✅ 不在文本生成完成时立即更新对话记录
- ✅ 改为在语音开始播放时更新对话记录
- ✅ 通过回调函数机制实现

### 方案2：修改字幕显示逻辑 ✅
- ✅ 字幕只显示当前正在播放语音的内容
- ✅ 基于 `animationState === 'speaking'` 状态来显示字幕
- ✅ 确保字幕与语音完全同步

### 方案3：确保动画状态正确 ✅
- ✅ `thinking` 状态：生成文本和语音时，显示动态图片
- ✅ `speaking` 状态：播放语音时，显示动态图片
- ✅ `static` 状态：空闲时，显示静态图片

## 实施步骤 ✅ 已完成

### 步骤1：修改对话记录更新时机 ✅
1. ✅ 修改 `GenerationManager.executeSpeakerTurn()` - 移除立即更新对话记录
2. ✅ 添加 `GenerationManager.generateAudioWithCallback()` - 新方法支持回调
3. ✅ 修改 `AudioManager.playAudio()` - 在语音播放开始时调用回调
4. ✅ 添加 `AudioItem.onPlaybackStart` 回调函数到类型定义

### 步骤2：确保字幕显示逻辑正确 ✅
1. ✅ 字幕只基于 `speakersState[speaker].animationState === 'speaking'` 显示
2. ✅ 从当前对话记录中找到对应角色的最新发言显示
3. ✅ 添加了 `.speaking` CSS 类，提供视觉强调效果

### 步骤3：验证动画状态转换 ✅
1. ✅ 确保状态转换序列正确：static → thinking → speaking → static
2. ✅ 确保图片选择逻辑与状态一致

## 代码修改文件 ✅

1. **types/index.ts** ✅ - 添加 `onPlaybackStart` 回调到 `AudioItem`
2. **utils/generation-manager.ts** ✅ - 修改对话更新时机，添加新的回调方法
3. **utils/audio-manager.ts** ✅ - 添加播放开始时的回调调用
4. **app/page.tsx** ✅ - 优化字幕显示逻辑，移除错误的方法调用
5. **app/globals.css** ✅ - 添加 `.speaking` 样式增强字幕效果

## 实际修改内容

### 1. types/index.ts
```typescript
export interface AudioItem {
  // ... 其他属性
  onPlaybackStart?: () => void;  // 新增回调函数
}
```

### 2. utils/generation-manager.ts
```typescript
// 移除了立即更新对话记录：
// this.updateConversation(speaker, text)

// 添加了新方法：
async generateAudioWithCallback(speaker: Speaker, text: string, sequenceNumber: number) {
  // 创建带回调的 AudioItem
  const audioItem: AudioItem = {
    // ...
    onPlaybackStart: () => {
      this.updateConversation(speaker, text)  // 在语音播放时才更新
    }
  }
}
```

### 3. utils/audio-manager.ts
```typescript
private async playAudio(audioItem: AudioItem): Promise<void> {
  // 在设置发言状态前先调用回调
  if (audioItem.onPlaybackStart) {
    audioItem.onPlaybackStart()  // 更新对话记录
  }
  this.speakerStateManager.setSpeaking(audioItem.speaker, audioItem.id)
}
```

### 4. app/page.tsx
```typescript
// 优化了字幕显示逻辑
const activeSpeaker = Object.keys(state.speakersState).find(speaker => 
  state.speakersState[speaker as Speaker]?.animationState === 'speaking'
) as Speaker | undefined;

// 更智能的提示信息
{Object.values(state.speakersState).some(s => s.animationState === 'thinking')
  ? 'Generating response...'
  : 'Preparing next speaker...'
}
```

## 预期结果 ✅ 已达成

- ✅ 字幕只在语音播放时显示
- ✅ 动态图片与语音播放状态一致
- ✅ 不会出现字幕先于语音显示的情况
- ✅ 只显示当前播放者的字幕，不显示历史字幕
- ✅ 提供了更好的视觉效果（.speaking 样式）
- ✅ 更智能的状态提示信息

## 测试建议

1. 启动应用并点击 "Start Discussion"
2. 观察主持人开始时：
   - 应该显示 "Generating response..." 而不是字幕
   - 图片应该是动态的（thinking 状态）
3. 当语音开始播放时：
   - 字幕应该同时出现
   - 图片仍然是动态的（speaking 状态）
   - 字幕有特殊的发光效果
4. 语音结束后：
   - 字幕消失
   - 图片变为静态
   - 显示 "Preparing next speaker..."
